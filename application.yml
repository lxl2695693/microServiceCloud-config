spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev
  application:
    name: microServiceCloud-config-dev
    
---
spring:
  profiles: test
  application:
    name: microServiceCloud-config-test